/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "qowgnoausww61ika.public.blob.vercel-storage.com" },
    ],
  },
};

export default nextConfig;
